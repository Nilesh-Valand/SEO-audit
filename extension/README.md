# SEO Audit — Chrome Extension

See the [root README](../README.md) for backend setup, load-unpacked steps, and Web Store packaging.

```bash
npm install
npm run build      # → dist/
npm run package    # build + zip → releases/seo-audit-extension-v*.zip
```
